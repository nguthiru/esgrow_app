import 'package:flutter/material.dart';

class EscrowPage extends StatelessWidget {
  const EscrowPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text("The escrow page"),
    );
  }
}
