import 'package:flutter/material.dart';

const double defaultPadding = 10.0;

const EdgeInsets allPadding = EdgeInsets.all(defaultPadding);
const EdgeInsets scrollPadding =
    EdgeInsets.fromLTRB(defaultPadding, defaultPadding, defaultPadding, 0);
